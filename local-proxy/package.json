{
  "name": "mft-local-proxy",
  "version": "1.0.0",
  "description": "Local MITM proxy — strips iframe-blocking headers from Kite, Sensibull & NiftyTrader so the Multi-Frame Trading dashboard works on any device without an extension.",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js"
  },
  "dependencies": {
    "http-mitm-proxy": "^0.9.2"
  },
  "engines": {
    "node": ">=16"
  }
}
